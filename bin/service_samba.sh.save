#!/bin/bash

/bin/osimpress_uteis_instalado.sh 1


cd /etc/samba
osimpress_uteis_backup.sh smb.conf

#opcao=1

#echo "Deseja resetar as configuracoes ?"
#echo "1-Sim"
#echo "2-Nao"
#echo -n "Opcao: "
#read reset

if [ $1 -eq 1 ]
then
    echo "[global]" > smb.conf
 -n "Opcao: "
#read reset

if [ $1 -eq 1 ]
then
    echo "[global]" > smb.conf
#!/bin/bash

/bin/osimpress_uteis_instalado.sh 1


cd /etc/samba
osimpress_uteis_backup.sh smb.conf

#opcao=1

#!/bin/bash

/bin/osimpress_uteis_instalado.sh 1


cd /etc/samba
osimpress_uteis_backup.sh smb.conf

#opcao=1

#echo "Deseja resetar as configuracoes ?"
#echo "1-Sim"
#echo "2-Nao"
#echo -n "Opcao: "
#read reset

if [ $1 -eq 1 ]
then
    echo "[global]" > smb.conf
#    echo -n "Digite o nome do grupo : "
#    read grupo
    echo "" >> smb.conf
    echo "	workgroup = $2" >> smb.conf
    echo "	security = user" >> smb.conf
#   echo "Qual o tipo de permissao ?"
#   echo "1-Publico"
#    echo "2-Usuario"
#    echo -n "Opcao: "
#    read permissao

    if [ $3 -eq 1 ]
    then
	 echo "	map to guest = Bad Password" >> smb.conf
    fi
	echo ""
fi

#echo -n "Digite o nome do compartilhamento: "
#read nome

#echo "Deseja criar uma nova pasta ?"
#echo "1-Sim"
#echo "2-Nao"
#echo -n "Opcao: "
#read nova

if [ $5 -eq 1 ]
	then
		echo $4
		mkdir /home/$4
		chmod 777 /home/$4
		hora="$(date +'%d-%m-%Y-%H:%M')"
		echo "Pasta $nome criada em $hora" > /home/$4/log.txt
    	fi

echo "[$4]" >> smb.conf
echo "    path=/home/$4" >> smb.conf
echo "    public=yes" >> smb.conf
echo "    writeable=yes" >> smb.conf
echo "	  avaiable = yes" >> smb.conf
echo "    browseable=yes" >> smb.conf
